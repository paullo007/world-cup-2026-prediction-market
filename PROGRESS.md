# PROGRESS — World Cup 2026 Prediction Market

_You-are-here for a cheap resume. Durable map: `CLAUDE.md`. Deep archive: `../SESSION MD CODE HISTORY/SESSION_LOG.md`._

**Last updated:** 2026-06-30 (end of Session 19 — **AUTOMATIC bracket advancement** + label renames: winners now auto-advance to the next slot derived from OUR resolved results (no ESPN later-round feed / id-map needed), shown across all 3 bracket views (Circle draws the advancing flag on the branch) + country pages, within a fetch of each completed match. Renamed bracket toggles → **FIFA/Date/Circle Bracket**, nav tab → **Brackets**. Shipped `main` HEAD = `92b6d25`.)
_Prior: Session 18 — bracket pinned-bar covers tabs on scroll; country-page knockout sections (most-recent-first). (`34ba268`)_

## Current phase
Session 19 **shipped and live**. Nothing half-done in code. (Tournament is in the **Round of 32** — knockout markets tradeable + auto-opening; winners auto-advance the bracket; 3 bracket views; country pages show knockout rounds.)

## START HERE (next action)
Nothing is in-flight. Pick whichever is most relevant:
1. 🔭 **Switch market auto-creation to derived advancement (recommended, Session 19 follow-up):** the BRACKET DISPLAY now advances winners from our own results (`getBracketTeams`), so it no longer needs ESPN's R16+ feed/`EVENT_TO_MATCH`. BUT `lib/knockoutMarkets.ensureKnockoutMarkets()` still reads teams via `fetchBracketTeams()` (ESPN) — so the tradeable R16+ MARKETS still wait on ESPN's (unverified) later-round ids. Change it to use `getBracketTeams()` so R16 markets auto-create from our derived advancement too (close the loop, no ESPN dependency). Until then: still re-verify the R16/QF/SF/Final `EVENT_TO_MATCH` ids before each round, OR make this switch.
2. **Pin-feature sweep** — Paul wants sticky/pinned headers "wherever there's a row of columns of data." Done on most (squad tables, Goals, Countries history, Rankings). Remaining candidate: **Portfolio** tables. (Standings = tiny 4-row per-group cards, pinning N/A.)
3. **Round-two country links** — extend `CountryLink` to the nested-anchor spots skipped in round one: market cards, the tradeable Bracket boxes, market question text, Portfolio. (Need no-nested-`<a>` handling.)
4. **SEC-01** — login rate-limit/lockout, still open (needs Upstash).
5. **next@16 migration** — still deferred (residual Next.js 14.2.x advisories).
6. **Architecture paper** — review v1.1 → v1.2 (`/plo-version-numbering`); now also undocumented: player pages, live-scores, the enriching generator, the 2-min cron, country links, the bracket auto-sync, the Rankings tab.
7. **Player/squad data freshness** — re-run `npx tsx scripts/gen-countries.ts` (fast; `.sdb-cache.json` committed) to refresh bio/club/photo as squads change.

## Done (high level)
- **Session 19 (Jun30.26) — automatic bracket advancement + renames:**
  - **Winners auto-advance from OUR results (NEW):** `lib/bracketSync.ts` `deriveBracketAdvancement(teams, winners)` — each later slot is labelled "Winner N"/"Loser N", so once match N resolves we fill the next slot ourselves (fixpoint loop → cascades R32→R16→QF…). `getBracketTeams()` = ESPN R32 draw + manual overrides + derived winners (**precedence: admin > derived > ESPN**). **No longer depends on ESPN's later-round feed or the unverified `EVENT_TO_MATCH` R16+ ids** to advance. Centralised: `/api/bracket-teams`, `app/bracket`, Matches day picker + All round labels (`app/page.tsx`), and country pages (`lib/countryKnockouts`) all call `getBracketTeams()`, so advancement shows everywhere within one fetch (~45s poll / 2-min settle) of full-time — zero manual steps.
  - **Circle view shows the move:** `BracketCircle` now draws the advancing flag on the branch it moves into (midpoint between the round node and its feeder), so a win visibly travels inward toward the trophy (inner rounds were dots-only before). Verified live: 90a=Canada, 91a=Brazil, 89a=Paraguay.
  - **Renames:** bracket toggles "Bracket by FIFA/Date/Circle" → **"FIFA Bracket" / "Date Bracket" / "Circle Bracket"**; nav tab "Bracket" → **"Brackets"** (display-only label in `CategoryNav`, route unchanged).
- **Session 18 (Jun30.26) — pinned-bar reclaims tab space + country knockout sections:**
  - **Bracket toggle bar covers the tabs on scroll:** new `useHeaderHeight()` (measures `#wc-header`, the black logo only — id added in `Navbar`); the bracket bar (now a plain `FitToWidth` sticky, `top=headerHeight`, `z-[45]` > nav z-40, opaque bg) rises over the category tab bar when scrolling, reclaiming that vertical space. (Was `StickyUnderNav` pinned below the full nav.)
  - **Country pages show knockout rounds:** NEW `lib/countryKnockouts.ts` `knockoutsForCountry(name)` — teams from the live bracket (`knockoutFixtures` + assignments, `canonicalTeam` match), results from resolved `KnockoutMatches` HOME markets (winner = which side resolved YES; level score ⇒ `pens`). `CountryDetail` extracts a shared **`FixtureCard`** (used by group + knockout) and renders one amber-titled `Box` per round the team reached ("ROUND OF 32 MATCHES" … "FINAL"/"THIRD-PLACE PLAY-OFF"); a round shows only once its opponent is known. Wired into `app/brazil` + `app/countries/[slug]`.
  - **Order = most recent first:** knockout sections render **above** Group Stage (which is over), and knockout rounds are sorted **Final → R32** (newest on top).
- **Session 17 (Jun28–30.26) — Bracket polish + pioneer nickname login:**
  - **All bracket country names clickable:** wrapped the shared bracket `Slot` team name in `CountryLink` (`components/bracketShared.tsx`) → every printed country, every box/round, both tree + date views + 3rd-place, AND teams that advance into later boxes, link to `/countries/<slug>`. Placeholders ("Winner 75") stay plain.
  - **Pioneer nickname login (live DB):** the 3 original email/password users now have a nickname so they can log in by typing it (keeping email+password as backup): `paul.lo@me.com`→**Paul**, `ahmad.dzuizz.annajib@gmail.com`→**Dzuizz**, `ainunnajib@gmail.com`→**Ainun** (all `nicknameLower` were free). Done via `scripts/enable-pioneer-nicknames.ts` (idempotent, by email, collision-checked). ⚠️ Left the SEPARATE `ainunnajib` nickname account (0 cash / ~2,378 in positions) untouched — Ainun now has two reachable accounts ("Ainun" = his email one, row 6; "ainunnajib" = the workaround).
  - **NEW "Bracket by Circle" view (3rd toggle, purple):** `components/BracketCircle.tsx` renders the knockout radially (viral circular-bracket design) — 32 R32 teams on the outer ring with flags + clickable `CountryLink` names (anchored outward so they don't overlap the flag, left-half flipped upright) + live/final score badges (green ring decided / red-pulse live), connectors spiralling inward through R16/QF/SF to a 🏆 at center. Reuses `useBracketScore`/`FitToWidth`; pure-geometry layout. `BracketLive` now has 3 views (fifa/date/circle); the linear round-label bar is hidden for the circle.
- **Session 16 (Jun27–28.26) — KNOCKOUTS TRADEABLE + Bracket/UI overhaul:**
  - **Knockout matches are tradeable (2-way Win/Lose):** every knockout tie (R32→Final + 3rd place) opens as two linked binary markets (`outcomeType` HOME/AWAY, **no DRAW** — knockouts can't end level), category **`KnockoutMatches`**, presented two-sided with live % like the group cards. Elo two-way **advancement** odds (`lib/elo.knockoutProbabilities`, neutral venue). **Resolution settles by who ADVANCES** (penalties included) via a new `advanceWinner` on `mergeForMarket` (ESPN's advancement flag, re-oriented), NOT the score-based `winner` (which would read a 1–1 shootout as DRAW). Ingest skips a knockout until the feed decides a winner. **R32 markets created live** (16 ties × 2 = 32 markets).
  - **Auto-create per round (zero manual step):** `lib/knockoutMarkets.ensureKnockoutMarkets()` (idempotent) is called best-effort at the end of `ingestAndPublish()`, so each round's markets self-open within minutes of the teams being confirmed (R16→QF→SF→Final). `scripts/create-knockout-markets.ts` is now a thin CLI wrapper/fallback over it.
  - **Penalty tag:** a resolved knockout with a level score + a winner shows "1 – 1 (pens)" / "{team} won (pens)" — derived, no schema change.
  - **Bracket tab dual views (toggle):** sticky toggle bar (left-aligned, under the nav with the round labels) — **🟢 "Bracket by FIFA"** = connected tree ordered by date (first match on top, `lib/bracket.bracketByDate`, still a valid tree so connectors line up) · **🔵 "Bracket by Date"** = flat schedule columns, no connectors (`bracketRoundsByDate` + `components/BracketSchedule`). Shared bits extracted to `components/bracketShared.tsx`. Box date stamp now **`Jun28/Sun/3:00am #73`** (nominal date + **local-tz kickoff, time bold+accent-blue** + match #) via `BoxDate`.
  - **Bracket boxes show scores + LIVE:** each box shows each team's goals on the right edge once live/completed (winner bold, loser dimmed) + a red **LIVE** badge in-progress. Sources: live feed (`useLiveScore`, via a new `LiveScoreProvider` wrapper) for playing/just-finished, else a server-fetched final-score map from resolved markets. `useBracketScore` hook + `ScoreMap` in bracketShared; both views + 3rd-place box.
  - **All tab → labeled sticky section bars:** "All Predictions Available" tab organized into full-width green sticky bars (`components/StickySectionBar`, same green as the FIFA button): **"{Round} Prediction Bets"** (knockout fixtures by date, round name derived from the live bracket so it tracks R32→R16…), **"World Cup Final Winner Prediction Bets"** (winner markets, implied **% high→low**, own section), **"Crazy Prediction Bets"**. Continuous card numbering. The All tab now KEEPS completed knockout/winner/crazy cards visible (badged) but excludes the finished group stage.
  - **Scores tab → per-day sections:** played matches grouped by **local day**, each headed by the same green sticky bar ("June 28 Matches:" …), newest day first (`components/ScoresByDay`).
  - **COMPLETED badge:** red "COMPLETED" (Brazil-card style) top-right on any resolved `MatchCard3Way`/`MarketCard` (global). Resolved `MarketCard`s reserve a top row so it doesn't overlap the title.
  - **Tab renames (display-only):** "All" → **"All Predictions Available"**, "Results" → **"Prediction Results"** (underlying category keys/routes unchanged; `LABELS` in `CategoryNav`).
- **Session 15 (Jun27.26) — player credits raised to 2,000 WC$ base:**
  - **+1,000 WC$ topped up to all 10 existing players** (flat add → leaderboard order preserved) via `scripts/topup-balances.ts` (dry-run default, `--apply`; live-DB write, committed for the record).
  - **New-signup base 1,000 → 2,000:** `prisma/schema.prisma` `balance @default(2000)` + `db:push` (plain `ALTER COLUMN SET DEFAULT`, existing rows untouched, RLS preserved). Signup/home copy updated (`app/register`, `app/login`, `app/page.tsx`, nickname-route comments).
  - **Leaderboard kept honest:** `app/leaderboard/page.tsx` `STARTING_BALANCE` 1000→2000 so P&L = `netWorth − 2000` still shows each player's true profit after the +1,000 bonus (existing players' green/red numbers unchanged; new players show +0). Subtitle → "Everyone is given 2,000 WC$." **Verified live by Paul — rankings + numbers all correct.** HEAD `c745a22`.
- **Session 14 (Jun24–26.26) — links, bet clarity, live bracket:**
  - **Country names link to their detail pages (NEW `components/CountryLink.tsx`):** subtle link (inherits color, hover underline) to `/countries/<slug>`, but ONLY when the name resolves to a real country page — "Draw", player names, and historical teams (West Germany, etc.) stay plain text; the fallback keeps the className so `flex-1 truncate` layout is preserved. Wired into Matches (`MatchCard3Way` header), Scores rows, Countries fixture opponents, Goals (`GoalscorersTable`), AI Knockouts (`AiKnockoutBracket` boxes + champion), Live strip. Standings was already linked. **Deferred (nested-anchor): market cards, Bracket tab, market question text, Portfolio.**
  - **Bet wording made explicit:** market page (`app/markets/[slug]`) now leads with a **green "what YES means" bar** above the title — color-matched to the YES/Buy buttons, showing the bet + live % via `yesClaim()`: "{Country} Will Win" / "The Match Will Be a Draw" / "{Country} Will Win the 2026 FIFA World Cup" (winner) / question-derived (crazy). Shown while open/awaiting. The **Matches-tab outcome pills** use the same wording globally ("{Country} Will Win" / "The Match Will Be a Draw", was "{Country} win" / "Draw").
  - **Bracket auto-populates from ESPN in real-time (NEW):** `lib/bracketSync.ts` `fetchBracketTeams()` pulls ESPN's knockout feed and maps each match to our bracket slot (`{num}a`/`{num}b`) via a **stable ESPN-event-id → match-number map** (`EVENT_TO_MATCH`). **R32 verified** against the live draw (ESPN placeholder labels → our slot labels, venue disambiguating the 4 fully-resolved ties — e.g. Netherlands/Morocco@Monterrey=75, Brazil/Japan@Houston=76); R16+ best-effort. Only confirmed real teams fill in (placeholders stay). `/api/bracket-teams` = ESPN merged with manual `BracketAssignment` overrides (admin wins). `components/BracketLive.tsx` (client) polls every **45s, 01:00–13:00 SGT, tab-visibility gated, hard-stop after Jul 20** — same cadence as the Matches live scores. **Display-only — never resolves/pays out.** 13 R32 teams populated live & verified.
  - **Bracket round-label bar pinned:** refactored `components/BracketTree.tsx` (now a client component) to the AI-Knockouts pattern — round labels moved into a `StickyUnderNav` row, body wrapped in `FitToWidth` — so Round-of-32…Final pins under the nav while scrolling.
  - **Rankings tab (NEW):** `/rankings` (nav pill after Standings) lists all 48 field teams by **official FIFA/Coca-Cola World Ranking** — a **static snapshot** (`lib/fifaRanking.ts`, 11 Jun 2026, frozen through the tournament) transcribed from inside.fifa.com (FIFA's API is browser-gated/unfetchable; ESPN & TheSportsDB don't carry the ranking). Shows the real world position (gaps where a non-qualified team ranks between, e.g. Italy #14) + points; columns are `#` (left) · `FIFA Ranking` (centered) · `Team` (left, `CountryLink`) · `Points` (left). **Pinned header** (`RankingsTable`, squad-table technique). Source note → FIFA page (`RANKINGS_SOURCES`). Sweden #36 included (it's in the app's field). Data spot-checked by Paul.
  - **Matches day-picker calendar fixed:** the calendar button used a `sr-only` date input (most browsers won't open the native picker on click). Now a real button triggers `el.showPicker()` (with `focus()` fallback), input moved to a sibling. ⚠️ The `useRef` must stay at the TOP of `MatchDayBoard` (before the early returns) — first attempt put it after, causing a Rules-of-Hooks crash ("Rendered more hooks…") that blanked the Matches tab; caught in testing.
  - **Knockout fixtures on the Matches day picker (NEW):** the picker stopped at Jun 28 because only the 72 group matches are tradeable markets. `lib/bracket.ts` `knockoutFixtures(teams)` flattens R32→Final + 3rd-place (teams from `fetchBracketTeams` + admin overrides); `app/page.tsx` builds them for the Matches tab; `MatchDayBoard` merges their (local-tz) kickoff days into the picker so it now spans **Jun 12 → Jul 20** and the `›` arrow advances the whole tournament. **`components/KnockoutCard.tsx`** = display-only card (round label + matchup [real team or "Winner 74" placeholder] + kickoff/venue), showing a **LIVE score + goalscorers** while playing / FT after via the shared `useLiveScore` + `GoalscorersBlock`. Not tradeable, never resolves.
  - **Real-time Standings (NEW):** `lib/standings.ts` `computeStandings(played, live)` merges in-progress group matches as **provisional points** (team ahead +3, level +1 each, with W/D/L/GF/GA). Only same-group fixtures not already resolved are merged → live knockout (cross-group) + already-played games never double-count. `components/StandingsLive.tsx` (client) recomputes from the 45s `LiveScoreProvider` feed; live-affected groups get a "Live" badge, rows a red dot + red points. `app/standings/page.tsx` wraps in `LiveScoreProvider`. Unit-tested (2-1→+3 live, 1-1→+1 each, cross-group ignored). No live markers showed at build time — group stage already finished.
  - **Shared live-window helper (`lib/liveWindow.ts`):** centralised `inLiveWindow()` (Singapore-time: polls 01:00–13:00 SGT, hard-stops **after Jul 20 SGT** — the Jul 19 final is Jul 20 SGT). **`LiveScoreProvider` (Matches/Scores/Standings) and `BracketLive` both use it now** — so every live surface stops after the tournament and the window logic can't drift. Fixed BracketLive's old UTC cutoff that would've stopped mid-window on the final day. 7/7 unit tests.
- **Session 13 (Jun22–24.26) — player pages + live scores + faster settlement:**
  - **Goalscorers on Countries match cards:** extracted the Matches-tab scorer rendering into a shared **`components/GoalscorersBlock.tsx`** (split home/away, minute, penalty/own-goal, assists, own-goal flag convention); `MatchCard3Way` + `CountryDetail` both use it. Threaded `scorers` through `resultsForCountry`/`MatchResult` (data was already fetched, just dropped). Country renders left, opponent right.
  - **"Trézéguet null" fix:** ESPN builds `fullName` as "first last", so a mononym with a null last name arrived as e.g. "Trézéguet null". Added **`cleanPlayerName()`** in `lib/results.ts` (strips standalone null/undefined tokens, collapses whitespace, falls back to `displayName`); `parseEspnScorers` uses it. Regression case added to `npm run test:scorers` (now 4/4). **Backfilled 4 live markets** (Trézéguet, Maurício + 2 trailing-space Vinícius Júnior) via `scripts/backfill-scorer-names.ts` (committed, dry-run default).
  - **Clickable player detail pages (NEW):** squad rows link to **`/countries/[slug]/[player]`** (`app/countries/[slug]/[player]/page.tsx`, force-dynamic) → **`components/PlayerDetail.tsx`**: bio (DOB/age/nationality/height/weight — ESPN, 100%), club + photo (TheSportsDB, best-effort), live **WC2026 goals + assists** (from our resolved-match data), "Full career stats on ESPN →" link, flag-avatar fallback when no photo. Career totals deliberately NOT shown (not in free feeds). `lib/countries.ts` gained `withPlayerSlugs`/`findPlayerBySlug`/`assistsForRoster` + an `enrichRoster()` so curated Brazil also gets bio/photo. `SquadTable` rows now clickable + Club/Assists columns populated.
  - **Enriching generator + alias pass:** rewrote **`scripts/gen-countries.ts`** to capture rich ESPN bio + best-effort TheSportsDB club/photo, throttled + retry-on-429 + **resumable on-disk cache `scripts/.sdb-cache.json`** (committed). Alias pass (country-nationality aliases e.g. Türkiye→Turkey, curated player aliases e.g. Zizo/Pedri/Uğurcan Çakır, name-trim) lifted **photos 85→95%, clubs 86→96%** (1246 players). `CountryPlayer.number/age/club` made optional (some players lack them).
  - **Live in-progress scores (NEW, display-only):** **`lib/liveScores.ts`** `fetchLiveScores()` keeps ESPN matches in state "in"/"post" (which the resolution path discards), parses live scorers (`parseEspnScorers`) + best-effort assists. **`/api/live-scores`** proxies it (force-dynamic, no cache). **`components/LiveScoreProvider.tsx`** polls every **45s**, gated on the SGT 01:00–13:00 window + tab visibility; `useLiveScore` handles ESPN home/away reversal. `MatchCard3Way` shows a red **LIVE score + minute + goalscorers** (and an FT-flash for just-finished-but-unresolved); **`components/LiveNowStrip.tsx`** adds a "LIVE NOW" strip to the Scores tab. **Strictly separate from resolution — never resolves/pays out** (settlement still only on `completed=true`). Verified against real live matches (France 3-0 Iraq, Mbappé/Dembélé w/ assists).
  - **2-min settlement during the SGT match window:** `.github/workflows/resolve-results.yml` gained a 2nd schedule `*/2 17-23,0-4 * * *` (= 01:00–13:00 SGT) that hits the **no-cooldown** `/api/refresh-results` so finished games pay out within ~2 min while matches play; the 15-min baseline still drives the cooldown-throttled `/api/auto-resolve` (the step branches on `github.event.schedule`). Chose `refresh-results` because `auto-resolve`'s `ingestIfDue` has a 3-min cooldown that would cap a 2-min cron. (GitHub sub-5-min crons are best-effort.)
- **Session 12 (Jun18–21.26) — UI features:**
  - **AI Knockouts overhaul:** (1) **Brazil vs Performance-adjusted** champion toggle (sticky blue buttons; "PREDICTED CHAMPION:" label now `text-accent` bold 1.25×). (2) **Performance-adjusted = market %×form** (`lib/dynamicPrediction.computeDynamic`: base = live winner-market %, ×form factor capped ±50% — same scale as market so 38% beats 16%; fixed the earlier Elo-softmax scale bug). Box shows both market % and performance-adjusted %. (3) **Elo-derived scorelines** (`eloScores` — margin scales with Elo gap + per-match nudge, winner preserved; 8 distinct scores vs mostly 2-1). (4) **Daily champion-change banner** — new `DailyChampion` table (UTC-day snapshot, RLS enabled) recorded on each `force-dynamic` page view; banner when today's perf-adjusted champion differs from the prior recorded day. No cron — recomputes live. (`prisma db push` applied to live DB.)
  - **Nav progress bar relocated + taller:** replaced the top-of-viewport `nextjs-toploader` with `components/NavProgress.tsx` — a gold 4px bar (1.25× the old 3px) pinned to the **bottom edge of the black header box**, sweeping left→right on internal nav, snapping to 100% on route change. Absolute overlay → no header-height change. (Navbar box now `relative`, hosts it via `Suspense`.) HEAD `61b90d2`.
  - **Compact global date format:** date stamps changed `Jun21.26/Sun` → **`Jun21/Sun`** (no year). `MatchStartTime` + `formatDate` (→ `Jul19/Sat`, used by Closes/Resolved displays). Day-picker calendar UI left as-is.
  - **Own-goal flag fix:** a goalscorer's flag now uses the player's OWN team (`flagTeamFor()` in `MatchCard3Way`), not the team the own goal counts for. FIFA convention: own goal sits in the beneficiary's tally + "(own goal)" but is attributed to the player's nationality (e.g. USA 2-0 Australia → 🇦🇺 Cameron Burgess 11' (own goal), under USA). HEAD `a49e27b`.
  - **Supabase RLS enabled (security fix):** Supabase emailed a Critical `rls_disabled_in_public` alert (public tables exposed via the anon-key PostgREST API). Enabled RLS on all 6 public tables (no policies → anon locked out); app unaffected since Prisma connects as the `postgres` owner role which bypasses RLS (no anon-key/supabase-js usage anywhere). Verified live reads + a rolled-back write. Live-DB change only (no code). ⚠️ RLS isn't in `prisma/schema.prisma` — re-enable if a table is ever dropped/recreated.
  - **Countries fixtures show results:** on the country detail page's Group Stage Matches, played games now show the **FT score oriented to the country + Won/Lost/Draw** and a red bold **"COMPLETED"** badge (top-right, timestamp size); not-yet-played games get a **bold green edge** (`border-2 border-yes`, the market green), rest of the box unchanged. New `resultsForCountry()` (lib/countries) joins fixtures to approved scores; Brazil + `/countries/[slug]` pages pass `results` to `CountryDetail`. HEAD `52ee672`.
  - **Bold kickoff time:** `MatchStartTime` (the single shared time renderer) now bolds + 1.15×-enlarges + colors **accent-blue (`text-accent`, matches the selected tab)** just the time portion; shows everywhere it's used. Times stay in the viewer's local timezone (not hardcoded Singapore). HEAD `426c9d6`.
  - **Top-10 scorers = all-time + live 2026:** the Goals-tab panel now adds each player's live World Cup 2026 goals to the ESPN all-time base (through 2022), matched by name, re-sorts by the new total, shows a green +N, and extends the World-Cups span to 2026 (Messi 13+3=16 → #2, Mbappé 12+2=14 → #5). `lib/topScorers.ts` = base; `app/goals/page.tsx` computes the per-player 2026 tally → `components/TopScorers.tsx` `wc2026` prop. Dynamic.
  - **MY WIN/LOSS on finished match cards (`MatchCard3Way`):** on the Matches tab, a resolved fixture's lower-left corner shows the signed-in user's net result — "MY WIN: X WC$" (green) / "MY LOSS: X WC$" (red) — instead of "X WC$ traded", when they hold a position. Net P&L summed across the fixture's Home/Draw/Away markets; `app/page.tsx` computes `myResultByMatch` (Matches tab only) → `MatchDayBoard` → `MatchCard3Way` `myResult` prop. Per-user, falls back to "traded" otherwise.
  - **Portfolio tables redesigned (`app/portfolio/page.tsx`):** prediction (YES/NO) is now its own column, split from the share count. Open positions: `Market | My Prediction | My Shares | Price | My Prediction Value | Value | My P&L`. Results: `Market | Outcome | My Prediction | My Shares | My Result | Payout | My P&L`, where **My Result = WIN/LOSS badge** (WIN when payout > 0). Helpers `PredictionCell`/`SharesCell`/`ResultBadge`; both YES and NO stack if a position holds both.
  - **Portfolio P&L extras:** **My Prediction Value** column (held shares × 1.00 = payout if right) + **Total Potential P&L** footer (Σ prediction value − Σ cost) on Open positions; **Total P&L** footer on Results. Each table now has an always-visible **FormulaBox** legend above it (terse `term = formula = numbers = result`; replaced the hover ⓘ tooltips which Paul disliked) whose worked example is **dynamic = the signed-in user's first row** (not fixed Brazil numbers). **Price shown to 2 decimals** (45.08%, transparency) instead of rounded.
  - **Goalscorers in Matches boxes:** `components/MatchCard3Way.tsx` — when a fixture is RESOLVED, a "Goalscorers" block at the bottom lists each scorer (team flag + player + minute, `(penalty)` after the time for penalty goals), sorted chronologically (stoppage-time aware). Reads the HOME market's existing `scorers` JSON.
  - **Split scorers home/away:** two-column layout — home-team scorers left, away-team right (matched via scorer `team` vs the fixture's home/away; empty column if a side didn't score). `ScorerLine` + `sameTeam` helpers.
  - **Goal assists (NEW pipeline bit):** ESPN's scoreboard feed has no assists, but its per-match *summary* endpoint does (scorer = `participants[0]`, assists = the rest). Added `fetchEspnAssists()`/`attachAssists()` in `lib/results.ts`, threaded the ESPN event id through `FinishedMatch`/`mergeForMarket`, and `ingestAndPublish` enriches scorers at resolve time (best-effort — never blocks resolution). UI shows each scorer's assist(s) indented + italic ("Assist: A, B"). **Backfilled** the 16 already-resolved matches (45 assists) into the live DB via a one-off script (idempotent resolution won't rewrite their scorers, so a backfill was required).
  - **Penalty goals were being dropped (BUG FIX):** ESPN types a scored penalty `"Penalty - Scored"` (no "goal"), so the scorer filter `/goal/i` silently skipped every penalty — scorers didn't reconcile with the score (England 4-2 showed 3 scorers; Kane's 12' penalty missing) and the "(penalty)" label never appeared. Fixed to `/goal|penalty/i` (`lib/results.ts`). **Re-backfilled** (rebuild full scorers, not just assists): 4 matches regained a penalty — Qatar-Switzerland (Embolo), Germany-Curaçao (Havertz), Austria-Jordan (Arnautović), England-Croatia (Kane).
  - **"Never-again" self-verify guard (the real fix):** the deeper problem was nothing checked scorers against the score, so gaps shipped silently. Three defenses: (1) **reconciliation self-check** in `ingest.ts` — if `scorers.length != homeGoals+awayGoals`, `console.warn` + append `⚠ scorers N/M` to `resultDetail` (skipped when no scorer data); (2) **own goals** now KEPT (the other silent gap) — `parseEspnScorers` credits them to ESPN's benefiting `detail.team.id`, rendered "(own goal)"; re-backfill restored **5 dropped own goals** (US-Paraguay, Qatar-Switzerland, Belgium-Egypt, Iraq-Norway, Austria-Jordan) → **0 matches now fail reconciliation**; (3) **regression test** — parser extracted to pure `parseEspnScorers`, `npm run test:scorers` asserts penalties/own-goals kept + missed-pens/cards excluded (3/3).
  - **"Top-10 Goal Scorers of All Time" panel:** `lib/topScorers.ts` + `components/TopScorers.tsx` — collapsible panel cloning the Countries "History of World Cup Winners" box (amber pill + sticky `<th>` headers via `useTopbarHeight` + `overflow-x:clip`). Static all-time men's WC scorers through 2022 (Klose 16, Ronaldo 15, …). **Lives on the GOALS tab** (`app/goals/page.tsx`) — was briefly shipped on Scores by mistake, then moved.
  - **Predict My Own Winner** (was Session 10, still live): users add a winner market for any team not pre-listed.
- **Session 11 (Jun17–18.26) — RESULTS PIPELINE FIX:**
  - **Root cause:** match results never auto-resolved. Not the fetch/match logic (that works in prod) — `resolveMarket`/`resolveMatchGroup` used Prisma **interactive** transactions, which fail intermittently over Supabase's transaction-mode pooler (`Transaction API error: Transaction not found`, P2028). Every resolve silently threw → matches stuck "Closed — awaiting result". Hidden by: Vercel cron **never enabled** (`crons:[]`) and the manual button being **login-gated** (logged-out clicks no-op'd). Diagnosed by deploying a temporary read-only `/api/diag-sources` (since removed) that proved prod fetch=16/17/5 OK but live resolve threw P2028.
  - **Fix — pooler-safe resolution:** `lib/trade.ts` now resolves via batched `$transaction([...])` (`resolveMarketOps()`: pre-read positions, then one atomic non-interactive batch). All trade + resolve transactions wrapped in `withTxRetry()` (retries P2028/connection drops; rebuilds ops each attempt since PrismaPromises are single-use). `executeTrade` kept interactive but now retried (a/ from this session).
  - **Fix — 3 robust triggers (never silently stall again):** (1) `<AutoResolve>` in layout pings throttled public `/api/auto-resolve` (`ingestIfDue()` — SystemState cooldown + only-if-stale) on every page view, refreshing if anything published; (2) GitHub Actions cron every 15m → `/api/auto-resolve` (`.github/workflows/resolve-results.yml`, no secret, plan-independent); (3) manual button **opened to everyone** (login gate removed from `/api/refresh-results`; toast now reports failures too).
  - **Backlog resolved live** + verified on the Scores tab. Temp diag endpoint removed.
- **Session 10 (Jun17.26):**
  - **Predict My Own World Cup Winner (NEW):** collapsible button atop the Tournament Winner tab, styled like the Countries tab's "History of World Cup Winners" panel (`components/PredictMyOwnWinner.tsx`). A signed-in user picks from a dropdown of the remaining 48-team field (with flags) — or types their own via **"Other"** — and `POST /api/winner-markets` creates a live binary "Will X win the 2026 FIFA World Cup?" market, then jumps to it.
  - **Elo-derived starting odds:** `lib/elo.tournamentWinProbability()` (Elo → field-normalized win share, host bonus, clamped to (0.002, 0.9); `DEFAULT_ELO` fallback for off-list custom teams). Seeded via `seedStateForProbability` (unlike the 11 hand-set winner seeds).
  - **Dedupe / aliases:** `lib/flags.canonicalTeam()` (USA↔United States, trim) — proposing an existing team links to it instead of duplicating; slug `@unique` is the backstop (P2002 → link). Validation: 2–40 chars, Latin letters incl. accents.
  - **Gate:** logged-in only, instant-create (no admin approval), no per-user limit — consistent with the app's zero-friction/auto-publish stance. First real submission writes a `Market` row to live Supabase.
  - Verified: `tsc --noEmit` clean, `npm run build` passes, collapsed/expanded screenshots, deploy green on prod domain.
- **Session 9 (Jun15–16.26):**
  - **Global nav progress bar:** `nextjs-toploader` in `app/layout.tsx` (gold `#fbbf24`, no spinner) — fires on every `<Link>`/back-forward nav. `position:fixed`, doesn't touch the sticky region.
  - **Results pipeline — 3rd source:** added **365Scores** (keyless, `webws.365scores.com`, WC competition id 5930, `statusGroup===4`=ended) as `fetch365Scores()` in `lib/results.ts`; pipeline now ESPN→365Scores→TheSportsDB and resolves on ANY single source (no "all agree" gate — that was always true; TheSportsDB is stale dead-weight). `/admin/sources` shows all 3. Resolved the stuck **Sweden 5–1 Tunisia** live (root cause was ESPN finalization timing, not the merge logic).
  - **Canada market (#11):** new "Will Canada win the 2026 FIFA World Cup?" tournament-winner market, seeded at 1% (Yes 1¢) like the other hand-set winners. In `prisma/seed.ts` + upserted live; `WINNER_ORDER` in `app/page.tsx` appended so it numbers #11. (Winner odds are hand-set, NOT Elo-derived; the live % is just the LMSR YES price moving with trades.)
  - **Sticky/fit pattern (NEW, reused 6×):** `components/FitToWidth.tsx` (scale-to-fit via CSS transform, min-scale floor → scroll), `useTopbarHeight()` + `StickyUnderNav` in `components/StickyUnderNav.tsx` (measure `#wc-topbar` height, pin just under nav). Applied to: AI Knockouts **bracket auto-fit** + **sticky round labels**; **Matches** date-selector bar; **Brazil/Countries squad** headers; **Countries history** headers; **Goals** headers. Sticky tables use `<th>`-level `position:sticky` + wrapper `overflow-x:clip` (never `auto`/`hidden`, which form a scroll container that breaks sticky).
  - **Countries tab (NEW):** pill after Brazil → `/countries` lists all 48 teams by group A–L; `/countries/[slug]` renders **identically to the Brazil tab** via shared `components/CountryDetail.tsx` + `components/SquadTable.tsx`. Squads for all 48 from ESPN (`scripts/gen-countries.ts` → `lib/countries.generated.ts`, 1246 players); `lib/countries.ts` `getCountry()` keeps Brazil curated (clubs/coach/5 titles), others from ESPN (club "—", no coach box — ESPN coach data is stale). `WORLD_CUP_TITLES` for the 7 past champions in the field.
  - **World Cup history (NEW):** collapsible "🏆 History of World Cup Winners" panel at top of `/countries` (`components/WorldCupHistory.tsx` + `lib/worldCupHistory.ts`) — all 22 finals 1930–2022, Brazil-titles-table style, with a **Final Score** column ("Winner W vs L Loser", amber "PENALTIES:" prefix for shootout finals).
  - **Flags:** `EXTRA_ISO` in `lib/flags.ts` for non-WC2026/historical countries (Italy, Chile, Czechoslovakia, Hungary, Russia, West Germany) — kept out of `ISO` so `ALL_TEAMS` stays the 48.
- **Session 8 (Jun14–15.26):** results-pipeline hardening (removed cooldown, idempotent resolution, per-match error isolation = CQ-01 fixed), FIFA black/gold redesign + single sticky top region, Elo-based varied starting odds (live reprice), architecture whitepaper v1.1.
- **Session 7 (Jun14):** auto-publish results, code audit (0 Crit/0 High), AI Knockouts tab, nickname-only login.
- **Sessions 1–6 (Jun11–13):** launch/deploy, 72 matches→3-way (216 markets), 48-team bracket, FIFA groups/times/venues, data tabs (Standings/Scores/Goals/Brazil), live resolutions. (Detail in `SESSION_LOG.md`.)

## In-flight
- None — everything committed, merged to `main`, pushed, deployed (Session 19 through `92b6d25`).

## Known issues / gotchas (see CLAUDE.md for full rules)
- 🟢 **Knockout markets = 2-way (no Draw), resolve by ADVANCEMENT** (Session 16). `KnockoutMatches` category; resolution uses `merged.advanceWinner` (ESPN flag) not the score-based `winner`; a penalty shootout pays the advancing team and shows "(pens)". Auto-created per round by `ensureKnockoutMarkets()` inside `ingestAndPublish` — so a wrong `EVENT_TO_MATCH` id for a later round would auto-create a MIS-PAIRED market; verify the id-map before each round (see START HERE #1).
- ⏰ **Bracket R16+ id-map is best-effort** (`lib/bracketSync.ts` `EVENT_TO_MATCH`). R32 verified; R16/QF/SF/Final event-id→match-num are placeholders while teams are TBD — **re-verify each before its round** (R16 ~Jul 4). Wrong now = no visible effect (teams TBD); wrong later = mis-placed teams. Also the 4 fully-resolved R32 ties (73/75/76/81) assume ESPN home→slot-a; spot-check orientation vs FIFA if one looks flipped.
- 🟢 **Bracket auto-fill is DISPLAY-ONLY + client-polled** (`/api/bracket-teams`, `BracketLive`, 45s, SGT window, hard-stop Jul 20). Never resolves/pays out. Manual `BracketAssignment` rows override ESPN (admin editor still works).
- ⛔ No commit/push or live-DB write without Paul's explicit OK.
- 🟡 **OneDrive resurrects deleted files** — the repo lives under OneDrive, which re-syncs locally-deleted files back to disk as **untracked** (e.g. `app/api/diag-sources/`, stray `scripts/*.ts`, `_tmp_*`/`_test_*`). Harmless to git/Vercel (untracked = not committed, not deployed) but can break a local `npm run build` (typechecks on-disk files). Stage only the specific files you mean; don't `git add -A`. A stale `.git/index.lock` from OneDrive can also block commits — safe to `rm` if no git process is running.
- ⛔ **Never use Prisma interactive transactions on the hot path** — they fail intermittently over the Supabase pooler (P2028). Use batched `$transaction([...])` + `withTxRetry()` (see CLAUDE.md hard rules). This is what broke all result resolution.
- 🟢 **Vercel cron does not fire on this plan** — results settle via on-view self-heal + GitHub Actions cron + the public button. **Two GH schedules now (Session 13):** baseline `*/15` → `/api/auto-resolve` (cooldown-throttled), plus `*/2 17-23,0-4` (= 01:00–13:00 SGT match window) → no-cooldown `/api/refresh-results`. GH sub-5-min crons are best-effort (delayed/coalesced under load), so 2-min is a target not a guarantee; on-view self-heal covers gaps.
- 🟢 **Live scores are DISPLAY-ONLY + client-side (Session 13)** — `lib/liveScores.ts` / `/api/live-scores` / `LiveScoreProvider` (45s poll, SGT-window + tab-visibility gated). NEVER resolves or pays out (settlement still only on `completed=true`). Because it's client polling, the live score only advances while a tab is open — by design (nobody watching = nobody to show). `useLiveScore` reorients ESPN home/away to our market's order.
- 🟢 **Player photos/clubs are best-effort (~95%/96%)** — bio is 100% (ESPN). Missing photos fall back to a flag avatar. `scripts/.sdb-cache.json` is committed so re-running `gen-countries.ts` is fast; alias maps for genuine name mismatches live in that script (`PLAYER_ALIAS`, `COUNTRY_NAT_ALIAS`).
- 🟢 **Sticky bars depend on the single `sticky top-0` wrapper having NO `overflow`/`transform` ancestor** (`app/layout.tsx`, now `id="wc-topbar"`). Sticky **table headers** need wrapper `overflow-x:clip`, never `auto`/`hidden`. `FitToWidth` uses `transform` → breaks `position:sticky` for descendants, so sticky bars live OUTSIDE it (`StickyUnderNav` wraps FitToWidth but the sticky is on its untransformed outer).
- 🟢 **Countries rosters are a static snapshot** (`lib/countries.generated.ts`) — now ESPN bio + TheSportsDB club/photo (Session 13); re-run `scripts/gen-countries.ts` to refresh. Non-Brazil still has no coach box (ESPN coach data stale).
- 🟠 **SEC-01** login rate-limit still open (Upstash). **next@16** migration deferred.
- 🟡 Nickname-only login has NO account security; auto-publish has no human gate (both accepted for play money).
- 🟢 144 legacy "Knockouts" markets hidden, not deleted. Elo reprice only touches OPEN untraded match markets.
- 🟢 NULL-outcome Prisma filter trap still applies (`app/page.tsx` `hideSecondary` uses `OR [HOME, null]`).
- 📝 `PROGRESS.md` + `CLAUDE.md` are now **committed to git** (changed Session 17; Paul OK'd it). Commit them with each save-phase. `.DS_Store` + `design-backups/` are untracked, ignore.

## How to run / verify
- `npm run dev` → :3000 · `npx tsc --noEmit` · `npm run build` · `npm run db:push` · `npm run db:seed`.
- **Regenerate squads (bio+club+photo):** `npx tsx scripts/gen-countries.ts` (writes `lib/countries.generated.ts`; fast — uses committed `.sdb-cache.json`; `--no-sdb` for ESPN-only).
- **Scorer parser test:** `npm run test:scorers` (must stay green when touching scorer parsing).
- **Live scores (read-only):** `curl <prod>/api/live-scores` — display feed; never resolves.
- **Elo reprice:** `npx tsx scripts/reprice-elo.ts` (dry-run) → `--apply` to write live.
- **One-off live scripts** (run with `set -a; source .env; set +a; npx tsx scripts/<file>`): `scripts/backfill-scorer-names.ts` (committed, dry-run default → `--apply`) used this phase to clean stored scorer names.
- **Visual check:** headless Chrome screenshot (Chrome-for-Testing at `~/.cache/puppeteer/...`; puppeteer-core installed in `/tmp/wcshot`).
- Full stack/commands in `CLAUDE.md`.
